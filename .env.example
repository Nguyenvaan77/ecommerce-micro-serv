# Local development only. Copy this file to .env before starting Compose.
# Do not reuse these credentials in shared or production environments.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# Change this value if port 5432 is already used on the host.
POSTGRES_PORT=5432
