# Nhật ký Phase 3 — 27-07-2026

## Thay đổi trong ngày

| Thời gian | Type | Nội dung | Kết quả / Kiểm chứng | Xác nhận |
| :--- | :--- | :--- | :--- | :--- |
| `14:43` | `PLANNING` | Chọn `user-service` làm service triển khai đầu tiên thay cho `auth-service`. | `user-service` sở hữu hồ sơ/business role; `auth-service` được hoãn đến khi ứng dụng gần hoàn chỉnh. | `ĐÃ XÁC NHẬN` — theo yêu cầu trực tiếp của người dùng |
| `14:43` | `PLANNING` / `DOCS` | Lập kế hoạch chi tiết Phase 3A cho `user-service`. | Đã xác định boundary, model, API MVP, schema, dependency, test, Definition of Done và các giả định cần duyệt tại [user-service-implementation-plan.md](../user-service-implementation-plan.md). | `ĐÃ XÁC NHẬN` — 14:59 27/07/2026 |
| `14:59` | `CODE` / `CONFIG` | Bắt đầu triển khai `user-service` theo kế hoạch đã duyệt. | Đang tạo Maven module, schema Liquibase, persistence, business logic và REST API; chưa có kết quả kiểm chứng cuối cùng. | `ĐÃ XÁC NHẬN` — 16:02:28 27/07/2026 |
| `15:00` | `TEST` | Kiểm tra Maven, Java và PostgreSQL local trước khi tích hợp. | Maven 3.9.9 và Java 21.0.9 sẵn sàng; Docker Desktop chưa chạy nên chưa thể kết nối PostgreSQL. Không có dữ liệu bị thay đổi. | `ĐÃ XÁC NHẬN` — 16:02:28 27/07/2026 |
| `15:00` | `PLANNING` / `DOCS` | Áp dụng các điều chỉnh trực tiếp của người dùng vào plan triển khai. | Chốt `Role` entity với `@ManyToMany`, update profile bằng `PUT`, endpoint gán `PM`/`ADMIN` cho local test và chưa viết automated test ở milestone này. | `ĐÃ XÁC NHẬN` — theo nội dung plan do người dùng chỉnh sửa và yêu cầu chạy |
| `15:03` | `CONFIG` / `TEST` | Khởi động lại Docker Desktop và PostgreSQL local. | Docker Engine 28.1.1 hoạt động; PostgreSQL container đã `healthy`, nhận kết nối tại `127.0.0.1:55432`; volume cũ được giữ nguyên. | `ĐÃ XÁC NHẬN` — 16:02:28 27/07/2026 |
| `15:08` | `CONFIG` / `DOCS` | Chuyển Maven module `user-service` ra root theo yêu cầu mới. | Module hiện nằm tại `user-service/`, cùng cấp với `docker-compose.yml`; root POM, parent relative path và plan đã cập nhật. | `ĐÃ XÁC NHẬN` — theo yêu cầu trực tiếp của người dùng |
| `15:12` | `CODE` / `CONFIG` | Hoàn thành source chính của `user-service`. | Đã có Maven module, Liquibase, entity/repository, DTO/validation, service/mapper, 5 REST endpoint, role seed và message Anh/Việt; Maven compile pass, đang chờ kiểm chứng runtime. | `ĐÃ XÁC NHẬN` — 16:02:28 27/07/2026 |
| `15:48` | `FIX` / `TEST` / `DOCS` | Hoàn tất rà soát và kiểm chứng khởi động `user-service`. | Root reactor `clean verify` thành công, 28 test hiện có pass; Liquibase/Hibernate khởi động thành công; health trả `UP`; role endpoint chỉ bật ở local profile và server bind `127.0.0.1`. Không viết/smoke test API theo yêu cầu hiện tại. | `ĐÃ XÁC NHẬN` — 16:02:28 27/07/2026 |

## Trạng thái hiện tại

- Quyết định `user-service` trước `auth-service`: `ĐÃ XÁC NHẬN`.
- Kế hoạch triển khai `user-service`: `ĐÃ XÁC NHẬN`.
- Implementation: `COMPLETED — ĐÃ XÁC NHẬN`.
- Thời gian nghiệm thu: `16:02:28 - 27/07/2026`.
- Git commit: chưa tạo.
