# Nhật ký Phase 2 — 27-07-2026

## Thay đổi trong ngày

| Thời gian | Type | Nội dung | Kết quả / Kiểm chứng | Xác nhận |
| :--- | :--- | :--- | :--- | :--- |
| `00:18:31` | `PLANNING` | Chuyển Phase 2 sang chiến lược cài hạ tầng theo nhu cầu, không dựng toàn bộ stack cùng lúc. | Milestone active được giới hạn ở Docker Compose và PostgreSQL; các thành phần khác có activation gate. | `ĐÃ XÁC NHẬN` — theo yêu cầu trực tiếp của người dùng |
| `00:20:37` | `PLANNING` / `DOCS` | Lập kế hoạch chi tiết Phase 2A cho Docker Compose và PostgreSQL. | Đã tạo checklist, phạm vi loại trừ, acceptance test, rủi ro và thứ tự kích hoạt. | `ĐÃ XÁC NHẬN` — người dùng yêu cầu triển khai ngày 27/07/2026 |
| `01:00:35` | `PLANNING` / `DOCS` | Đơn giản hóa Phase 2A thành PostgreSQL local chỉ dùng superuser. | Bỏ application role, database riêng và init script; giữ Compose, volume, health check, Makefile và runbook. | `ĐÃ XÁC NHẬN` — theo yêu cầu trực tiếp của người dùng |
| `01:02:37` | `TEST` | Kiểm tra tĩnh Compose và Makefile lần đầu. | Compose hợp lệ và chỉ có `postgres`; tùy chọn phụ `config --networks` không được Compose 2.35 hỗ trợ; GNU Make gọi Git Bash và lỗi signal pipe trên Windows. Không có dữ liệu bị xóa. | `ĐÃ XÁC NHẬN` — 01:12:13 27/07/2026 |
| `01:03:59` | `TEST` | Pull image và khởi động PostgreSQL lần đầu. | Image, default network và named volume được tạo; container chưa chạy vì host port `5432` đang được PID 8200 sử dụng. Chưa có dữ liệu PostgreSQL được ghi. | `ĐÃ XÁC NHẬN` — 01:12:13 27/07/2026 |
| `01:05:24` | `TEST` | Chạy `make restart` để kiểm tra persistence. | Make dừng trước khi gọi Docker vì `.env` dùng comment `//`, không hợp lệ với dotenv/Make; container và dữ liệu không bị thay đổi. Đã đổi comment sang cú pháp `#`. | `ĐÃ XÁC NHẬN` — 01:12:13 27/07/2026 |
| `01:06` | `CONFIG` / `FIX` | Hoàn thiện Compose, `.env`, `.env.example` và Makefile tối giản. | Compose chỉ còn PostgreSQL; Make dùng `cmd.exe` trên Windows; port local đổi sang `55432`; `make clear` từ chối chạy khi thiếu `CONFIRM=YES`. | `ĐÃ XÁC NHẬN` — 01:12:13 27/07/2026 |
| `01:07` | `TEST` | Kiểm chứng PostgreSQL và persistence. | PostgreSQL 16.14 healthy; SQL chạy bằng `postgres`; dữ liệu smoke test tồn tại qua `make restart` và `make down`/`make up`. | `ĐÃ XÁC NHẬN` — 01:12:13 27/07/2026 |
| `01:08` | `TEST` / `DOCS` | Dọn smoke test và hoàn thiện runbook/checklist. | Bảng smoke test đã xóa; host TCP `127.0.0.1:55432` kết nối được; volume/network tồn tại; runbook đã ghi đủ lệnh vận hành. | `ĐÃ XÁC NHẬN` — 01:12:13 27/07/2026 |

## Trạng thái hiện tại

- Kế hoạch chi tiết Phase 2A: `ĐÃ XÁC NHẬN`.
- Hạ tầng Phase 2A: `COMPLETED — ĐÃ XÁC NHẬN`.
- Thời gian nghiệm thu: `01:12:13 - 27/07/2026`.
- PostgreSQL: đang chạy, `healthy`, version 16.14.
- Git commit: chưa tạo.
